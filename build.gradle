subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
        jcenter()
    }

    dependencies {
        compile("org.springframework.boot:spring-boot-starter:1.3.1.RELEASE")
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.9'
}
